{
  "name": "lead-nurturing-automation",
  "version": "2.0.0",
  "description": "CRM lead analytics dashboard + AWS Lambda API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand",
    "deploy": "cd lambda && zip -r ../lambda.zip . ../crm.js && echo 'Upload lambda.zip to AWS Lambda'"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "fast-check": "^4.7.0",
    "jest": "^30.3.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
