# ── CRM Integration (required) ───────────────────────────
CRM_BASE_URL=http://your-frappe-crm-host:8000
CRM_USER=your_frappe_email@example.com
CRM_PASSWORD=your_frappe_password

# ── Server (optional) ────────────────────────────────────
PORT=3001

# ─────────────────────────────────────────────────────────
# NOTE: Slack, Telegram, Email, WhatsApp, SMS credentials
# are configured directly in the n8n workflow — not here.
# See workflows/lead-nurturing-workflow.json for placeholders.
# ─────────────────────────────────────────────────────────
