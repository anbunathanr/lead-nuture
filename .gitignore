# Dependencies
node_modules/
package-lock.json

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
!.env.example

# Secret files
*.pem
*.key
*.p12
*.crt

# Scripts (contain hardcoded credentials — local use only)
scripts/

# SAM build artifacts
.aws-sam/

# Lambda copied files (generated during deploy)
lambda/bedrock-client.js
lambda/workflow-template.json
workflows/lead-nurturing-workflow.json
workflows/email-reply-workflow.json
workflows/telegram-bot-workflow.json
workflows/fibonacci-nudge-workflow.json

# Tenant config and data (contain secrets and crawled content)
data/tenant-config.json
data/knowledge-base.json
data/nudge-schedule.json
data/replied-emails.json

# Customer data (contain credentials — never commit)
data/customers/

# Personal credentials reference (never commit)
docs/my-credentials.md

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo
